# Flowmap `.mmd` authoring spec — moved

The canonical authoring spec now lives with the tooling so it versions and
ships as one unit (and can't drift from the validator that enforces it):

→ **[`tools/SYNTAX_README.md`](tools/SYNTAX_README.md)**

It is bundled inside the installable `flowmap-spec-tools` package, so a repo
that installs the tools gets the matching syntax spec automatically.
