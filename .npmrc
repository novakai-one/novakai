# Force npm to install devDependencies even when NODE_ENV=production is set.
# The Claude desktop app exports NODE_ENV=production to processes it spawns;
# npm reads that as --omit=dev and silently drops dev deps (vite, ts-morph, etc.)
# on any `npm install`. This line neutralises that. Do not delete.
include=dev
