// @flowmap-node main__firstRender kind=function
/** render, breadcrumb, mermaid sync, baseline history, then open unfold unconditionally — boot has no surface decision (M4 correction) — and load optional bodies.json */
export function main__firstRender(): void {
  throw new Error('unimplemented');
}
