// @flowmap-contract plan__PlanLayoutNode kind=type
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import type { PlanLayoutNode } from './plan__PlanLayoutNode';
export type _keys_PlanLayoutNode = keyof PlanLayoutNode;
