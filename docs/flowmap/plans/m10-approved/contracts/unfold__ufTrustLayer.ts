// @flowmap-node unfold__ufTrustLayer kind=function
/** trust layer with an OPTIONAL advisory source: same-origin edge-advisory-allowlist.txt when present, a Load button otherwise; absent source = layer disabled, never wrong — advisory edges dash amber, advisory descs are marked */
export function trustLayer(): void {
  throw new Error('unimplemented');
}
