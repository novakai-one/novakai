import type { Hier } from './__types.generated';

// @flowmap-node mermaid__parseGroupDirective kind=function
/** parse one %% group / %% group-member dialect line into the hier overlay; the pipeline parser mirrors this grammar and parser-conformance holds the two together (A3) */
export function parseGroupDirective(_line: string, _hier: Hier): boolean {
  throw new Error('unimplemented');
}
