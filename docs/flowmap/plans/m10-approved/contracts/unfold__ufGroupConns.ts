// @flowmap-node unfold__ufGroupConns kind=function
/** external connections of a container: every edge with exactly one endpoint inside the subtree, aggregated to the coarsest foreign container and weight-summed — the same aggregation grammar as the stage pills */
export function groupConns(_id: string): { uses: [string, number][]; usedBy: [string, number][] } {
  throw new Error('unimplemented');
}
