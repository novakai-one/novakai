// @flowmap-node unfold__ufClose kind=function
/** leave unfold for the legacy-compare view (temporary, until M5 parity deletes the canvas): save the ViewSpec, hand the selection back, hide the overlay — invoked ONLY by the explicit compare affordance; no surface choice is recorded, no ✕ and no Esc route here (M4 correction) */
export function close(): void {
  throw new Error('unimplemented');
}
