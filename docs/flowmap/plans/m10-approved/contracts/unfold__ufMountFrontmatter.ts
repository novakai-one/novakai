// @flowmap-node unfold__ufMountFrontmatter kind=function
/** mount the app's frontmatter editor (panel/inspector-frontmatter) for the selected node inside the reading inspector — the same hooks write path as renameInPlace; commits re-derive the folded view */
export function mountFrontmatter(_host: HTMLElement, _id: string): void {
  throw new Error('unimplemented');
}
