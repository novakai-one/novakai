// @flowmap-contract viewspec__ViewModelIndex kind=type
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import type { ViewModelIndex } from './viewspec__ViewModelIndex';
export type _keys_ViewModelIndex = keyof ViewModelIndex;
