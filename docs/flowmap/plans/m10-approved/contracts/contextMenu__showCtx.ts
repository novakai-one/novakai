import type { CtxEntry } from './__types.generated';

// @flowmap-node contextMenu__showCtx kind=function
/** render the menu items at a clamped position near the cursor */
export function showCtx(_clientX: number, _clientY: number, _items: CtxEntry[]): void {
  throw new Error('unimplemented');
}
