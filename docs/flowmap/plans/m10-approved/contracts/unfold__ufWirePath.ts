import type { Box } from './__types.generated';

// @flowmap-node unfold__ufWirePath kind=function
/** one wire geometry, not two: reading-mode wires from the app's real port-side geometry (core/state portPos + bestSides) and the render/wires elbow path — replaces the local orthoPath */
export function wirePath(_a: Box, _b: Box): string {
  throw new Error('unimplemented');
}
