// @flowmap-node unfold__ufDockState kind=type
/** the panel chrome state: active tab, collapsed flag, width in px — the only dock state, persisted whole under unfold.dock */
export interface DockState {
}
