import type { Frontmatter } from './__types.generated';

// @flowmap-node frontmatter__applyFrontmatterLine kind=function
/** fold a parsed line into a per-node frontmatter accumulator */
export function applyFrontmatterLine(_acc: Record<string, Frontmatter>, _parsed: { id: string; key: string; value: string; iface?: number }): void {
  throw new Error('unimplemented');
}
