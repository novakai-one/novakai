// @flowmap-contract avoidWorker__RoutedPoly kind=type
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import type { RoutedPoly } from './avoidWorker__RoutedPoly';
export type _keys_RoutedPoly = keyof RoutedPoly;
