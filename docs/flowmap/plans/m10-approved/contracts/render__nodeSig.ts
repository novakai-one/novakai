import type { DiagramNode } from './__types.generated';

// @flowmap-node render__nodeSig kind=function
/** structural signature; when unchanged the inner DOM is left alone */
export function nodeSig(_n: DiagramNode, _id: string, _isContainer: boolean, _traced: string | null): string {
  throw new Error('unimplemented');
}
