// @novakai-contract files__saveMmd kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { saveMmd } from './files__saveMmd';
export type _p_saveMmd = Parameters<typeof saveMmd>;
export type _r_saveMmd = ReturnType<typeof saveMmd>;
