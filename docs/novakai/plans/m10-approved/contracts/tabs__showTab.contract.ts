// @novakai-contract tabs__showTab kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { showTab } from './tabs__showTab';
export type _p_showTab = Parameters<typeof showTab>;
export type _r_showTab = ReturnType<typeof showTab>;
