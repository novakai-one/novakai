import type { AppContext, CameraApi, ViewApi } from './__types.generated';

// @novakai-node view__initView kind=function
/** own the drill level and breadcrumb DOM bound to ctx and camera */
export function initView(_ctx: AppContext, _camera: CameraApi): ViewApi {
  throw new Error('unimplemented');
}
