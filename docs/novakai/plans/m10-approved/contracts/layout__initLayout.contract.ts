// @novakai-contract layout__initLayout kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initLayout } from './layout__initLayout';
export type _p_initLayout = Parameters<typeof initLayout>;
export type _r_initLayout = ReturnType<typeof initLayout>;
