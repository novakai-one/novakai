import type { DiffInput, MmdDiff } from './__types.generated';

// @novakai-node diff__diffModels kind=function
/** compare two models by node id and edge key; return added, removed and changed nodes and edges */
export function diffModels(_before: DiffInput, _after: DiffInput): MmdDiff {
  throw new Error('unimplemented');
}
