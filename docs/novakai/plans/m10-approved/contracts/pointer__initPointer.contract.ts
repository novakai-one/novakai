// @novakai-contract pointer__initPointer kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initPointer } from './pointer__initPointer';
export type _p_initPointer = Parameters<typeof initPointer>;
export type _r_initPointer = ReturnType<typeof initPointer>;
