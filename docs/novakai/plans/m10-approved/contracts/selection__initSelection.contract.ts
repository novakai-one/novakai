// @novakai-contract selection__initSelection kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initSelection } from './selection__initSelection';
export type _p_initSelection = Parameters<typeof initSelection>;
export type _r_initSelection = ReturnType<typeof initSelection>;
