// @novakai-contract camera__toWorld kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { toWorld } from './camera__toWorld';
export type _p_toWorld = Parameters<typeof toWorld>;
export type _r_toWorld = ReturnType<typeof toWorld>;
