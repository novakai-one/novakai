// @novakai-contract state__sliceStubs kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { sliceStubs } from './state__sliceStubs';
export type _p_sliceStubs = Parameters<typeof sliceStubs>;
export type _r_sliceStubs = ReturnType<typeof sliceStubs>;
