// @novakai-node theming__applyFont kind=function
/** apply a font choice app-wide from the single FONTS source: sets --node-font (legacy canvas) and --uf-font (the primary surface consumes it with the current stack as fallback), persists the pref */
export function applyFont(_key: string): void {
  throw new Error('unimplemented');
}
