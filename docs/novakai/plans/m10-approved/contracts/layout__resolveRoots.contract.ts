// @novakai-contract layout__resolveRoots kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { resolveRoots } from './layout__resolveRoots';
export type _p_resolveRoots = Parameters<typeof resolveRoots>;
export type _r_resolveRoots = ReturnType<typeof resolveRoots>;
