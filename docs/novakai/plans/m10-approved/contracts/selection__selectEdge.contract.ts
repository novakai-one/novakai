// @novakai-contract selection__selectEdge kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { selectEdge } from './selection__selectEdge';
export type _p_selectEdge = Parameters<typeof selectEdge>;
export type _r_selectEdge = ReturnType<typeof selectEdge>;
