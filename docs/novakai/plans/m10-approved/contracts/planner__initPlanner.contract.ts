// @novakai-contract planner__initPlanner kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initPlanner } from './planner__initPlanner';
export type _p_initPlanner = Parameters<typeof initPlanner>;
export type _r_initPlanner = ReturnType<typeof initPlanner>;
