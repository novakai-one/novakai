// @novakai-contract navigator__initNavigator kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initNavigator } from './navigator__initNavigator';
export type _p_initNavigator = Parameters<typeof initNavigator>;
export type _r_initNavigator = ReturnType<typeof initNavigator>;
