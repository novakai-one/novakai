import type { AppContext, MermaidApi, PlannerApi } from './__types.generated';

// @novakai-node planner__initPlanner kind=function
/** build the full-screen build-plan review surface: own SVG canvas, status overlay, tri-pane sync, accept/reject with coherence, gated export */
export function initPlanner(_ctx: AppContext, _deps: { mermaid: MermaidApi }): PlannerApi {
  throw new Error('unimplemented');
}
