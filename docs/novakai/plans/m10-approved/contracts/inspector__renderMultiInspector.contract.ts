// @novakai-contract inspector__renderMultiInspector kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { renderMultiInspector } from './inspector__renderMultiInspector';
export type _p_renderMultiInspector = Parameters<typeof renderMultiInspector>;
export type _r_renderMultiInspector = ReturnType<typeof renderMultiInspector>;
