// @novakai-contract persistence__loadPrefs kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { loadPrefs } from './persistence__loadPrefs';
export type _p_loadPrefs = Parameters<typeof loadPrefs>;
export type _r_loadPrefs = ReturnType<typeof loadPrefs>;
