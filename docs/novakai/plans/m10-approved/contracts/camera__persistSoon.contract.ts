// @novakai-contract camera__persistSoon kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { persistSoon } from './camera__persistSoon';
export type _p_persistSoon = Parameters<typeof persistSoon>;
export type _r_persistSoon = ReturnType<typeof persistSoon>;
