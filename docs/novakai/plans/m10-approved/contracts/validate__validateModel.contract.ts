// @novakai-contract validate__validateModel kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { validateModel } from './validate__validateModel';
export type _p_validateModel = Parameters<typeof validateModel>;
export type _r_validateModel = ReturnType<typeof validateModel>;
