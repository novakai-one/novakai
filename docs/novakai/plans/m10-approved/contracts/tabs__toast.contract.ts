// @novakai-contract tabs__toast kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { toast } from './tabs__toast';
export type _p_toast = Parameters<typeof toast>;
export type _r_toast = ReturnType<typeof toast>;
