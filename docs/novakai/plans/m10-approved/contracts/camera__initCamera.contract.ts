// @novakai-contract camera__initCamera kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initCamera } from './camera__initCamera';
export type _p_initCamera = Parameters<typeof initCamera>;
export type _r_initCamera = ReturnType<typeof initCamera>;
