// @novakai-node unfold__ufSelectGroup kind=function
/** a group is a first-class selectable (U6): select + inspect, never stage (U8 deferred) — clears wire/type focus, relights, re-renders tree + inspector in either projection */
export function selectGroup(_id: string): void {
  throw new Error('unimplemented');
}
