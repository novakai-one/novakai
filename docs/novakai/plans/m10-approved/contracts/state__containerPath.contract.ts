// @novakai-contract state__containerPath kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { containerPath } from './state__containerPath';
export type _p_containerPath = Parameters<typeof containerPath>;
export type _r_containerPath = ReturnType<typeof containerPath>;
