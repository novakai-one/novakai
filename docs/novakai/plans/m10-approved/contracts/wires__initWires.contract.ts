// @novakai-contract wires__initWires kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initWires } from './wires__initWires';
export type _p_initWires = Parameters<typeof initWires>;
export type _r_initWires = ReturnType<typeof initWires>;
