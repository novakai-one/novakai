// @novakai-contract avoidRouter__handleReply kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { handleReply } from './avoidRouter__handleReply';
export type _p_handleReply = Parameters<typeof handleReply>;
export type _r_handleReply = ReturnType<typeof handleReply>;
