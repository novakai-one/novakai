// @novakai-contract layout__isSpineEdge kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { isSpineEdge } from './layout__isSpineEdge';
export type _p_isSpineEdge = Parameters<typeof isSpineEdge>;
export type _r_isSpineEdge = ReturnType<typeof isSpineEdge>;
