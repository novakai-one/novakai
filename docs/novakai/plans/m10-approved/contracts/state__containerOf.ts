import type { StateStore } from './__types.generated';

// @novakai-node state__containerOf kind=function
/** nearest non-group ancestor (the drill level a node lives at) or null */
export function containerOf(_state: StateStore, _id: string): string | null {
  throw new Error('unimplemented');
}
