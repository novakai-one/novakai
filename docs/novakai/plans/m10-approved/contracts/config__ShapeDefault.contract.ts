// @novakai-contract config__ShapeDefault kind=type
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import type { ShapeDefault } from './config__ShapeDefault';
export type _keys_ShapeDefault = keyof ShapeDefault;
