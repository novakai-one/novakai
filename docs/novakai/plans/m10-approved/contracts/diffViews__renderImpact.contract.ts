// @novakai-contract diffViews__renderImpact kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { renderImpact } from './diffViews__renderImpact';
export type _p_renderImpact = Parameters<typeof renderImpact>;
export type _r_renderImpact = ReturnType<typeof renderImpact>;
