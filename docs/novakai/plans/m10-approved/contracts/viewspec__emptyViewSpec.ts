import type { ViewSpec } from './__types.generated';

// @novakai-node viewspec__emptyViewSpec kind=function
/** the fresh-view spec: fully folded, nothing hidden or selected, calls layer ON (approved decision: wires are the story, never an opt-in) */
export function emptyViewSpec(): ViewSpec {
  throw new Error('unimplemented');
}
