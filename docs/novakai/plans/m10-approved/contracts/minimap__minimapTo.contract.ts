// @novakai-contract minimap__minimapTo kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { minimapTo } from './minimap__minimapTo';
export type _p_minimapTo = Parameters<typeof minimapTo>;
export type _r_minimapTo = ReturnType<typeof minimapTo>;
