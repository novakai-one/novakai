// @novakai-contract nodes__makeEdge kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { makeEdge } from './nodes__makeEdge';
export type _p_makeEdge = Parameters<typeof makeEdge>;
export type _r_makeEdge = ReturnType<typeof makeEdge>;
