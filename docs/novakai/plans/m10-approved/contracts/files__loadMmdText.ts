// @novakai-node files__loadMmdText kind=function
/** replace the diagram from .mmd text: apply through the mermaid module (the only parse path) then zoom to fit — the one code path behind the legacy hidden input and the unfold io tab */
export function loadMmdText(_text: string): void {
  throw new Error('unimplemented');
}
