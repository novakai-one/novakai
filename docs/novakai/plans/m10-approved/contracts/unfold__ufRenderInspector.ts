// @novakai-node unfold__ufRenderInspector kind=function
/** empty until selection; then kind, crumbs, desc, interfaces, blast count, connections, and the real source body from ctx.bodies — a selected wire resolves its endpoints and underlying relations through the same pure lift decision the painter draws (ufLiftWires), so the two surfaces cannot disagree */
export function renderInspector(): void {
  throw new Error('unimplemented');
}
