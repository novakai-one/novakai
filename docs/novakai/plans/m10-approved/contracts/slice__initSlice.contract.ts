// @novakai-contract slice__initSlice kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initSlice } from './slice__initSlice';
export type _p_initSlice = Parameters<typeof initSlice>;
export type _r_initSlice = ReturnType<typeof initSlice>;
