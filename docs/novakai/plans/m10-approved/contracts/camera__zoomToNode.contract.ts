// @novakai-contract camera__zoomToNode kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { zoomToNode } from './camera__zoomToNode';
export type _p_zoomToNode = Parameters<typeof zoomToNode>;
export type _r_zoomToNode = ReturnType<typeof zoomToNode>;
