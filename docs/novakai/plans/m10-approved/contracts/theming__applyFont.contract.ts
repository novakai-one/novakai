// @novakai-contract theming__applyFont kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { applyFont } from './theming__applyFont';
export type _p_applyFont = Parameters<typeof applyFont>;
export type _r_applyFont = ReturnType<typeof applyFont>;
