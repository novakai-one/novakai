import type { Frontmatter } from './__types.generated';

// @novakai-node frontmatter__isFrontmatterEmpty kind=function
/** true when every frontmatter field is blank (decides whether to persist) */
export function isFrontmatterEmpty(_fm: Frontmatter | undefined): boolean {
  throw new Error('unimplemented');
}
