// @novakai-node unfold__ufLiftedWire kind=type
/** one drawn wire: sibling anchors, summed weight, concealed endpoint count (the badge), atomic (arrowed reveal), hot (selection highlight), advisory, and the underlying model relations */
export interface LiftedWire {
}
