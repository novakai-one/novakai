import type { ParseResult } from './__types.generated';

// @novakai-node mermaid__fromMermaid kind=function
/** parse Mermaid text into a model fragment: nodes, edges, geometry, frontmatter, kinds, roots */
export function fromMermaid(_text: string): ParseResult {
  throw new Error('unimplemented');
}
