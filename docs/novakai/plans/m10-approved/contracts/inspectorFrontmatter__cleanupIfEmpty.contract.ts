// @novakai-contract inspectorFrontmatter__cleanupIfEmpty kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { cleanupIfEmpty } from './inspectorFrontmatter__cleanupIfEmpty';
export type _p_cleanupIfEmpty = Parameters<typeof cleanupIfEmpty>;
export type _r_cleanupIfEmpty = ReturnType<typeof cleanupIfEmpty>;
