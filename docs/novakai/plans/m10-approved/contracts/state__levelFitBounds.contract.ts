// @novakai-contract state__levelFitBounds kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { levelFitBounds } from './state__levelFitBounds';
export type _p_levelFitBounds = Parameters<typeof levelFitBounds>;
export type _r_levelFitBounds = ReturnType<typeof levelFitBounds>;
