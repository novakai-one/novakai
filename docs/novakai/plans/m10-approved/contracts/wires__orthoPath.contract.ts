// @novakai-contract wires__orthoPath kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { orthoPath } from './wires__orthoPath';
export type _p_orthoPath = Parameters<typeof orthoPath>;
export type _r_orthoPath = ReturnType<typeof orthoPath>;
