// @novakai-contract keyboard__initKeyboard kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initKeyboard } from './keyboard__initKeyboard';
export type _p_initKeyboard = Parameters<typeof initKeyboard>;
export type _r_initKeyboard = ReturnType<typeof initKeyboard>;
