// @novakai-contract inspectorFrontmatter__wire kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { wire } from './inspectorFrontmatter__wire';
export type _p_wire = Parameters<typeof wire>;
export type _r_wire = ReturnType<typeof wire>;
