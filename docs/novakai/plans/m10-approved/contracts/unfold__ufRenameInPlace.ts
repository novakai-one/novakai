// @novakai-node unfold__ufRenameInPlace kind=function
/** inline rename on the selected card (Enter / double-click on selected), writing through the existing model path — mutate ctx.state then hooks render + sync + pushHistory + persist, never a private write path */
export function renameInPlace(_id: string): void {
  throw new Error('unimplemented');
}
