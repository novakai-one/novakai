// @novakai-node unfold__ufSelectSync kind=function
/** selection survives the mode boundary: seed SEL from the editor selection on open; hand the reading selection back (selectOnly + zoomToNode via deps) on close — no new state, one shared selection */
export function selectSync(_dir: 'open' | 'close'): void {
  throw new Error('unimplemented');
}
