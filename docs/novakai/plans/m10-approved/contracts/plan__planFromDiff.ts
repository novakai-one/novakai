import type { DiffInput, Plan } from './__types.generated';

// @novakai-node plan__planFromDiff kind=function
/** D2 unified review — derive a Plan from a raw before/after map diff so the planner reviews pasted proposals, not only authored plans */
export function planFromDiff(_before: DiffInput, _after: DiffInput, _base: string): Plan {
  throw new Error('unimplemented');
}
