// @novakai-contract diffViews__renderSplit kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { renderSplit } from './diffViews__renderSplit';
export type _p_renderSplit = Parameters<typeof renderSplit>;
export type _r_renderSplit = ReturnType<typeof renderSplit>;
