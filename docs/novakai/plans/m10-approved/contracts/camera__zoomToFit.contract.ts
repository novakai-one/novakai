// @novakai-contract camera__zoomToFit kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { zoomToFit } from './camera__zoomToFit';
export type _p_zoomToFit = Parameters<typeof zoomToFit>;
export type _r_zoomToFit = ReturnType<typeof zoomToFit>;
