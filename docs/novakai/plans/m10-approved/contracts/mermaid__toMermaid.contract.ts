// @novakai-contract mermaid__toMermaid kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { toMermaid } from './mermaid__toMermaid';
export type _p_toMermaid = Parameters<typeof toMermaid>;
export type _r_toMermaid = ReturnType<typeof toMermaid>;
