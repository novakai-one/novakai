// @novakai-contract mermaid__fromMermaid kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { fromMermaid } from './mermaid__fromMermaid';
export type _p_fromMermaid = Parameters<typeof fromMermaid>;
export type _r_fromMermaid = ReturnType<typeof fromMermaid>;
