// @novakai-contract tabs__togglePanel kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { togglePanel } from './tabs__togglePanel';
export type _p_togglePanel = Parameters<typeof togglePanel>;
export type _r_togglePanel = ReturnType<typeof togglePanel>;
