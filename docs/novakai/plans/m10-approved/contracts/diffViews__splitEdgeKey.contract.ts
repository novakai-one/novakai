// @novakai-contract diffViews__splitEdgeKey kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { splitEdgeKey } from './diffViews__splitEdgeKey';
export type _p_splitEdgeKey = Parameters<typeof splitEdgeKey>;
export type _r_splitEdgeKey = ReturnType<typeof splitEdgeKey>;
