// @novakai-contract diffViews__renderList kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { renderList } from './diffViews__renderList';
export type _p_renderList = Parameters<typeof renderList>;
export type _r_renderList = ReturnType<typeof renderList>;
