// @novakai-contract selection__clearSel kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { clearSel } from './selection__clearSel';
export type _p_clearSel = Parameters<typeof clearSel>;
export type _r_clearSel = ReturnType<typeof clearSel>;
