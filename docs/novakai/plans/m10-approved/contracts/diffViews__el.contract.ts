// @novakai-contract diffViews__el kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { el } from './diffViews__el';
export type _p_el = Parameters<typeof el>;
export type _r_el = ReturnType<typeof el>;
