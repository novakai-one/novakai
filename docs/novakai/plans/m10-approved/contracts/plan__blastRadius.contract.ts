// @novakai-contract plan__blastRadius kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { blastRadius } from './plan__blastRadius';
export type _p_blastRadius = Parameters<typeof blastRadius>;
export type _r_blastRadius = ReturnType<typeof blastRadius>;
