// @novakai-contract inspector__updateStatus kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { updateStatus } from './inspector__updateStatus';
export type _p_updateStatus = Parameters<typeof updateStatus>;
export type _r_updateStatus = ReturnType<typeof updateStatus>;
