// @novakai-contract nodes__initNodes kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { initNodes } from './nodes__initNodes';
export type _p_initNodes = Parameters<typeof initNodes>;
export type _r_initNodes = ReturnType<typeof initNodes>;
