// @novakai-contract camera__applyCam kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { applyCam } from './camera__applyCam';
export type _p_applyCam = Parameters<typeof applyCam>;
export type _r_applyCam = ReturnType<typeof applyCam>;
