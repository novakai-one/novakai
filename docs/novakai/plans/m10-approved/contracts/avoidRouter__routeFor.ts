import type { Point } from './__types.generated';

// @novakai-node avoidRouter__routeFor kind=function
/** cached polyline for an edge, dropped if the obstacle field changed since it was computed */
export function routeFor(_id: string, _sig: string): Point[] | null {
  throw new Error('unimplemented');
}
