// @novakai-contract pointer__startMarquee kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { startMarquee } from './pointer__startMarquee';
export type _p_startMarquee = Parameters<typeof startMarquee>;
export type _r_startMarquee = ReturnType<typeof startMarquee>;
