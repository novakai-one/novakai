// @novakai-contract avoidRouter__sanitizeRect kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { sanitizeRect } from './avoidRouter__sanitizeRect';
export type _p_sanitizeRect = Parameters<typeof sanitizeRect>;
export type _r_sanitizeRect = ReturnType<typeof sanitizeRect>;
