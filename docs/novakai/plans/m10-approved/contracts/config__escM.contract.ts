// @novakai-contract config__escM kind=function
// Compile-time contract. Drift in a member name / arity / return breaks typecheck.
// TODO(Idea A): add executable behavioral assertions under a test runner.
import { escM } from './config__escM';
export type _p_escM = Parameters<typeof escM>;
export type _r_escM = ReturnType<typeof escM>;
