# io/layout.ts readability pass (M6 p1)

Files touched: `src/io/layout.ts` only.

Score: 95 -> 92 (byRule before: max-lines-per-function 3, id-length 89, max-params 1,
sonarjs/cognitive-complexity 2; after: max-lines-per-function 1, id-length 89, max-params 2,
sonarjs/cognitive-complexity 0).

Complexity reductions:
- `placeSatellites` (cognitive complexity 25 -> part of new lower-complexity split): extracted
  `spineCrossBounds`, `partitionSatellites`, `placeLooseSatellites`, `placeSatelliteClusters` as
  module-private helpers; `placeSatellites` now just wires them together.
- `autoLayout` (cognitive complexity 24 -> part of new lower-complexity split): extracted
  `resolveSpine`, `layerSpine`, `inlineMixedGroupSatellites`, `positionSpineLayers`,
  `clusterCandidateGroups`, `markReferenceEdgesOrtho`; `autoLayout` is now a linear pipeline call
  sequence with a single early return.
- All 21 functions in the file now report cognitive complexity < 15 (max remaining is 14, on the
  pre-existing `findBackEdges`, untouched by this pass).
- Added a one-line `//` comment above `initLayout` (the file's only exported function), which
  previously lacked one.

No exported name/signature/type changed; no test file touched; diff is 151+/75- on the target
(within the 300-line budget). All extracted helpers stay in `src/io/layout.ts` as module-private
functions inside `initLayout`'s closure (same access pattern as the pre-existing helpers).
