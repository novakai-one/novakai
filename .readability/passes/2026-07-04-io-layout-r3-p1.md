# src/io/layout.ts readability pass (r3-p1)

Files touched: `src/io/layout.ts`

Score: 92 -> 43 (id-length 89 -> 40; max-lines-per-function 1 -> 1 unchanged;
max-params 2 -> 2 unchanged, left in place per pass orders).

No complexity reductions attempted this pass (scope restricted to renames +
duplicate-string hoists only; no `sonarjs/no-duplicate-string` warnings were
present on this file, so no hoists were needed).

Renamed ~19 unclear single-letter LOCAL variables/loop bindings across
~15 module-private helpers (footprint, captureGroups, spineNodeSet,
anchorOf, findBackEdges, assignLayers, orderByBarycenter,
spineCrossBounds, partitionSatellites, placeLooseSatellites,
placeSatelliteClusters, the anchorMain/lay/placeOne closures inside
placeSatellites, wrapGroups, inlineMixedGroupSatellites,
positionSpineLayers, clusterCandidateGroups, markReferenceEdgesOrtho) to
descriptive names (`node`, `size`, `groupId`, `groupNode`, `edge`, `rootId`,
`sat`, `anchorId`, `queue`, `sweep`, `member`, `next`).

Left unfixed (by design): every function-parameter id-length warning
(named functions, arrow-const helpers, and anonymous callback params —
`e`, `s`, `a`, `b`, `p`, `L`, etc.), the 4 `w`/`h` object-literal-key
warnings tied to the local `Footprint` shape and the external
`nodeFootprint`/`DiagramNode` field names, the 2 `w`/`h` assignment-target
warnings on `DiagramNode` fields, `max-params` (arity change forbidden),
and `max-lines-per-function` on `initLayout` (out of scope this pass).
No signatures changed; API surface hashes for `src/io` identical.
